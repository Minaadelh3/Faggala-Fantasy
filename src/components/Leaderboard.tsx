import { Trophy } from 'lucide-react';
export interface LeaderboardRow { rank:number; name:string; points:number }
export default function Leaderboard({rows=[]}:{rows?:LeaderboardRow[]}){return <div className="panel"><div className="section-title"><h3>Leaderboard</h3><span className="text-xs text-midnight-600">Current</span></div>{rows.length?<ul className="divide-y divide-mist">{rows.map(row=><li key={`${row.rank}-${row.name}`} className="flex items-center justify-between py-2"><div className="flex items-center gap-3"><span className={row.rank===1?'rank-medal':'grid h-7 w-7 place-items-center text-xs'}>{row.rank}</span><span className="text-sm font-medium">{row.name}</span>{row.rank===1&&<Trophy size={14} className="text-gold"/>}</div><strong>{row.points.toLocaleString()}</strong></li>)}</ul>:<p className="empty-copy">Rankings are published after scoring.</p>}</div>}
