import{useEffect,useState,type ReactNode}from'react';import{AlertTriangle,Clock3,LoaderCircle,LockKeyhole}from'lucide-react';import{cairoDate}from'../lib/format';import type{Gameweek}from'../types/database';
export function LoadingScreen({label='Loading your fantasy world…'}:{label?:string}){return <div className="min-h-[50vh] grid place-items-center"><div className="text-center text-midnight-600"><LoaderCircle className="animate-spin mx-auto mb-3 text-gold"/><p>{label}</p></div></div>}
export function EmptyState({title,message,action}:{title:string;message:string;action?:ReactNode}){return <div className="panel py-12 text-center"><div className="mx-auto mb-3 h-11 w-11 rounded-full bg-gold/10 grid place-items-center"><AlertTriangle className="text-gold-dim" size={20}/></div><h2 className="font-display font-bold text-lg text-midnight-900">{title}</h2><p className="text-sm text-midnight-600 mt-1 max-w-md mx-auto">{message}</p>{action&&<div className="mt-5">{action}</div>}</div>}
export function ErrorNotice({message}:{message:string}){return <div role="alert" className="rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-800">{message}</div>}
export function PageHeader({eyebrow,title,children}:{eyebrow?:string;title:string;children?:ReactNode}){return <header className="flex flex-wrap items-end justify-between gap-4 mb-6"><div>{eyebrow&&<p className="eyebrow">{eyebrow}</p>}<h1 className="font-display text-2xl md:text-3xl font-bold text-midnight-900">{title}</h1></div>{children}</header>}
export function DeadlinePanel({gameweek,action}:{gameweek:Gameweek;action?:ReactNode}){const[now,setNow]=useState(Date.now());useEffect(()=>{const timer=window.setInterval(()=>setNow(Date.now()),60000);return()=>window.clearInterval(timer)},[]);const delta=new Date(gameweek.transfer_deadline).getTime()-now;const open=delta>0&&gameweek.status==='upcoming';const days=Math.max(0,Math.floor(delta/86400000));const hours=Math.max(0,Math.floor(delta%86400000/3600000));const mins=Math.max(0,Math.floor(delta%3600000/60000));const soon=open&&delta<86400000;return <section className={`round-command ${soon?'is-soon':''}`} aria-label="Current round and deadline"><div className="round-orbit"/><div className="relative"><p className="eyebrow text-gold-light">Round {String(gameweek.number).padStart(2,'0')} · {gameweek.status}</p><h2>{gameweek.name??`Gameweek ${gameweek.number}`}</h2><p className="round-date">Deadline · {cairoDate(gameweek.transfer_deadline)}</p></div><div className="relative flex flex-wrap items-center gap-4 md:gap-7">{open?<div><p className="round-status"><Clock3/>Deadline in</p><p className="countdown"><span>{days}<small>D</small></span><span>{hours}<small>H</small></span><span>{mins}<small>M</small></span></p></div>:<div><p className="round-status"><LockKeyhole/>Team locked</p><p className="text-sm text-white/60 mt-1">Changes reopen next round.</p></div>}{action}</div></section>}
export function Modal({title,children,onClose}:{title:string;children:ReactNode;onClose:()=>void}){return <div className="modal-backdrop" role="presentation" onMouseDown={e=>{if(e.currentTarget===e.target)onClose()}}><section className="fantasy-modal" role="dialog" aria-modal="true" aria-labelledby="modal-title"><div className="section-title"><h2 id="modal-title">{title}</h2><button className="text-link" onClick={onClose}>Close</button></div>{children}</section></div>}
